# Base URL of the Sarko Delivery backend (NestJS, global prefix /v1 is added by the app).
NEXT_PUBLIC_API_URL=http://localhost:3000

# Google OAuth web client ID. Leave unset to show the Google button as a
# disabled "coming soon" placeholder on the login page.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
