# Request: Firebase cleanup and sync-code fixes

On 2026-07-17, the user asked to verify and fix the large uncommitted changes another AI agent had made wiring both Flutter apps' services to the local Drift database (which had caused issues with chats and notifications not being visible), find and replace leftover unused Firebase-era code now that the backend runs on Node and Postgres, and avoid running any emulator. The landing page redesign and shop subdomain web app frontend were logged separately in the landing-redesign-and-shop-webapp folder.
