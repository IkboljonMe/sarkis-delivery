name: customer_app
description: Sarkis Bread - customer delivery app.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_messaging: ^15.1.3

  provider: ^6.1.2
  pin_code_fields: ^8.0.1
  http: ^1.2.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  shimmer: ^3.0.0
  fluttertoast: ^8.2.4
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
